# ADR-0005: Native Git Through Structured Processes

**Status:** Accepted

Use the installed Git executable through Execa argument arrays with shell execution disabled.
Expected-ref, ancestry, containment, timeout, and redaction checks surround every mutation.
