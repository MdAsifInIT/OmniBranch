# Phase 8 Plan

1. Add public installer target, scope, plan, operation, receipt, evidence, journal, and error contracts plus JSON Schema.
2. Implement provider discovery and exact user/project destination resolution with deterministic deduplication.
3. Implement payload enumeration, frontmatter/reference/hash validation, containment, and integrity evidence.
4. Implement receipt-backed plan/install/update/status/doctor/rollback/uninstall with locks, staging, journals, backups, and recovery.
5. Test every target, conflict mode, lifecycle transition, failure boundary, hostile path, and concurrent installer.
