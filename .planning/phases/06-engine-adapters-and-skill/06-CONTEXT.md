# Phase 6 Context

All engines share one lifecycle and normalized result boundary. Version, cancellation, and policy controls must be verified before autonomous launch; otherwise the adapter produces a guided handoff. Provider command lines are configurable compatibility data rather than guessed constants.

The canonical skill lives in `skills/omnibranch`; generated provider layouts are derived from it. Codex probing is attempted locally. Claude Code, OpenCode, and Antigravity remain fixture-verified until installed.
