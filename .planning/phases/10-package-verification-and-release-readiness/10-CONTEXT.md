# Phase 10 Context

The release gate must prove the exact npm tarball from an empty prefix, create deterministic supply-chain artifacts, document every install/recovery path, and define three-OS Node 22 CI and approval-gated provenance without publishing anything.

Local verification may use the registry only to install the pinned native dependency and query advisories. Provider engines and GitHub live writes remain external gates and cannot be reported as passed.
