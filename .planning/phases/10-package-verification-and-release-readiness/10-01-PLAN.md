# Phase 10 Plan

1. Build and inspect the npm tarball allowlist and production manifest.
2. Install the tarball into an empty temporary prefix and exercise version, targets, dry-run, install, doctor, uninstall, rollback, and final cleanup.
3. Generate deterministic skill/plugin archives, CycloneDX SBOM, and SHA-256 checksums.
4. Update installation, compatibility, examples, upgrade, rollback, limitations, changelog, changeset, and release documentation.
5. Extend three-OS Node 22 CI and add protected-environment, explicit-input npm provenance publishing.
6. Run dependency, security, release, package, and complete repository gates; audit all 0.2 requirements and record external verification limits.
