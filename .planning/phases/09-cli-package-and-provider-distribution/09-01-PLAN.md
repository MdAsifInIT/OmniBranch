# Phase 9 Plan

1. Expose `skill targets|plan|install|status|update|doctor|rollback|uninstall` through the stable CLI envelope and structured installer errors.
2. Resolve the embedded canonical payload in both source and bundled package execution, including project-root discovery.
3. Convert the CLI workspace into public `omnibranch@0.2.0` with Node 22 metadata, an explicit file allowlist, tsup bundling, and only the native SQLite dependency external.
4. Update canonical skill metadata and generate complete, identical provider skill trees without `OMNIBRANCH.md` copies.
5. Add and validate the Claude plugin and `omnibranch-tools` marketplace layout.
6. Add CLI, layout, contract, and package-content tests; review and run the complete repository gate before committing.
