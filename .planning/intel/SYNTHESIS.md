# OmniBranch Documentation Synthesis

The documentation consistently specifies a local-first 0.1 CLI that coordinates bounded work across isolated Git worktrees. Correctness belongs to a deterministic event-driven control plane; AI engines and external services are capability-driven adapters whose claims require independent validation. The milestone progresses from contracts/configuration through persistence/Git, orchestration, mock execution, external adapters, skill packaging, and release hardening.

The authoritative precedence used for synthesis is architecture decisions, normative specifications, project charter, then implementation guides. No locked contradictions remain after normalizing approval terminology and deferring the illustrative daemon.
