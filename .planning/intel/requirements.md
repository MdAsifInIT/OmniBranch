# Ingested Requirements

The 0.1 release must provide strict configuration validation, repository discovery, event replay, SQLite projections, safe Git/worktree operations, ownership and lease enforcement, deterministic scheduling, policy approvals, validation evidence, resumable mock execution, GitHub sandbox integration, engine adapter contracts, the OmniBranch skill, cross-platform CI, hostile-input coverage, and release artifacts.
