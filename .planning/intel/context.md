# Ingested Context

OmniBranch begins as a documentation-only repository. The implementation follows the dependency order in the build guide and backlog, proving an offline deterministic mock-worker vertical slice before enabling GitHub or live AI adapters.
