# Policy

Policy recognizes repository reads/writes, local execution, Git reads/safe writes/destructive writes, network reads/writes, secret reads, plugin loads, SCM mutations, and cloud mutations. Deny rules win. Approvals bind action, target, requester, approver class, decision, and expiry. Workers cannot approve their own requests.
