# Adapters

Adapters follow `probe → prepare → launch → supervise → collect → finalize`. Capability values are native, adapted, unsupported, or unknown. Unknown version, cancellation, policy control, or result boundaries require guided mode. Codex, Claude Code, OpenCode, and Antigravity CLI prefer supervised CLI surfaces; Antigravity IDE is guided by default.
